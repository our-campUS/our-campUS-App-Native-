pluginManagement {
    includeBuild("../node_modules/@react-native/gradle-plugin")
}

plugins {
    id("com.facebook.react.settings")
}

extensions.configure(com.facebook.react.ReactSettingsExtension){ ex ->
    ex.autolinkLibrariesFromCommand()
}

dependencyResolutionManagement {
    // ❌ 이 줄 삭제해야 함
    // repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    // repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    
    repositories {
        google()
        mavenCentral()
        maven { url = "https://www.jitpack.io" }

        // Kakao SDK repository (groups/public이 kakao-public + kakaopay-public 포함)
        maven { url = 'https://devrepo.kakao.com/nexus/content/groups/public/' }

        // AsyncStorage v3 local Maven repo
        maven { url = new File(rootProject.projectDir, "../node_modules/@react-native-async-storage/async-storage/android/local_repo") }
    }
}

rootProject.name = 'ourCampusApp'
include ':app'
includeBuild('../node_modules/@react-native/gradle-plugin')
